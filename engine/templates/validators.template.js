// clients/{{SLUG}}/validators.template.js — Anti-alucinação específica (opcional)
module.exports = {
  // validate: (response, config) => { valid: boolean, reason?: string, replacement?: string }
  // blockedPatterns: [] — regex adicionais
};
