// clients/{{SLUG}}/prompts.js — System prompt e overrides por cliente (opcional)
module.exports = {
  // getSystemPrompt: (config) => string
  // buildDynamicPrompt: (inventory, customerName, isReturning, stockCounts, session, diasRestantes) => string
};
